import { AbstractStrategy } from './abstract.strategy';
import { IDistVariable } from '../../../interfaces';
export declare class ManualStrategy extends AbstractStrategy {
    generate(variable: IDistVariable): Promise<string>;
}
