import { AbstractStrategy } from './strategies';
import { IArguments, ISettings } from '../../interfaces';
export declare const DEFAULT_DIST = ".env.dist";
export declare const DEFAULT_PATH = ".";
export declare const DEFAULT_PATTERN = ".env";
export declare const ENV_VAR = "NODE_ENV";
export declare class Generator {
    private readonly strategy;
    private readonly fileManager;
    private readonly envParser;
    private existentVariables;
    constructor(strategy: AbstractStrategy);
    getStrategy(): AbstractStrategy;
    generate(settings: ISettings, args: IArguments): Promise<void>;
    private getExistentVariable;
}
