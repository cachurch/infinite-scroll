import { IDistVariable, IEnvVariable } from '../../interfaces';
export declare class EnvParser {
    private readonly countEnvGroups;
    private readonly countDistGroups;
    private readonly countDistGroupsWithDefault;
    private readonly distRegex;
    private readonly envRegex;
    parseDist(line: string): IDistVariable | null;
    parseEnv(line: string): IEnvVariable | null;
}
