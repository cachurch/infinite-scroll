"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const readline = require("readline");
const enums_1 = require("../enums");
class Console {
    constructor() {
        this.process = readline.createInterface({
            input: process.stdin,
            output: process.stdout
        });
    }
    writeLine(text, color = enums_1.Colors.Reset) {
        console.log(`${color}${text}${enums_1.Colors.Reset}`);
    }
    readLine(text, defaultValue = '') {
        this.process.write(defaultValue);
        return new Promise(resolve => this.process.question(`${text}${enums_1.Colors.Reset}`, resolve));
    }
    info(text) {
        this.writeLine(text, enums_1.Colors.FgGreen);
    }
    error(text) {
        this.writeLine(text, enums_1.Colors.FgRed);
    }
    warning(text) {
        this.writeLine(text, enums_1.Colors.FgYellow);
    }
    exit() {
        this.process.close();
    }
}
exports.Console = Console;
//# sourceMappingURL=console.js.map